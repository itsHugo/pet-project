export * from '../lib/models/index'
export {
    Router, IRouter, Request, Response,
    get, post, put, del, head, opt
} from 'ccd'
export { BaseController } from '../lib/baseController';
// export let Router = express.Router;